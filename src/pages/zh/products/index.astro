---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { t } from '@/lib/i18n';
import ProductsIndexContent from '@/components/ProductsIndexContent.astro';

const locale = 'zh';
---

<BaseLayout title={`产品 — ${t(locale, 'brand')}`} description={t(locale, 'products.subtitle')} locale={locale}>
  <ProductsIndexContent locale={locale} />
</BaseLayout>
